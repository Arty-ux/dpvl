<div class="container" {{#if (not this.nonav)}}id="{{this.link}}"{{/if}}>
    <div class="panel panel-default">
        <div class="panel-heading">
            <div class="text-center">
                <a class="btn btn-info" role="button" data-toggle="collapse" href="#collapse-{{@index}}" aria-expanded="false" aria-controls="collapse-{{@index}}">Mehr über {{title}} <span class="caret"></span></a>
            </div>
        </div>
        <div class="panel-body collapse" id="collapse-{{@index}}">
            <div class="row">
                <div class="col col-xs-6">
                    <div class="image-area">
                        <img src="assets/images/{{image}}" class="img-responsive">
                    </div>
                </div>
                <div class="col col-xs-6">
                    <div class="content-area">
                        {{#block "main"}}{{{contents}}}{{/block}}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>