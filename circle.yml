general:
  branches:
    only:
      - master
machine:
  timezone: Europe/Vienna
  node:
    version: 5.4.0
dependencies:
  override:
    - sudo apt-get update; sudo apt-get install g++
    - npm install
    - npm install -g gulp
test:
  override:
    - echo "Add tests!"
deployment:
  production:
    branch: master
    commands:
      - npm install -g gulp
      - ./scripts/deploy_production.sh